---
general:
  build_dir: .

machine:
  python:
    version: pypy-2.2.1
  environment:
    AWS_DEFAULT_REGION: eu-west-1

dependencies:
  pre:
    - sudo pip install moto moto pytest pytest-pep8 pytest-cov
test:
  override:
    - py.test -v --ignore=venv --cov . --pep8 .
